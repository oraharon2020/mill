// ===== הגדרות המסך =====
// זה הקובץ היחיד שצריך לערוך.

window.APP_CONFIG = {
  // שם היעד שמוצג מעל העיגול
  goalName: "הון עצמי לדירה",

  // תחנות בדרך למיליון הראשון (בש"ח)
  milestones: [100000, 250000, 500000, 750000, 1000000],

  // גודל כל "רמה" אחרי המיליון הראשון
  levelSize: 1000000,

  // מזהה סודי של הנתונים שלך — משמש כמפתח באחסון.
  // חובה להחליף למחרוזת אקראית ארוכה משלך (לפחות 8 תווים,
  // אותיות/מספרים/מקפים). ליצירה: openssl rand -hex 16
  dataId: "CHANGE-ME-to-a-long-random-string",

  // הסנכרון עובד אוטומטית כשהאתר רץ על Vercel עם Upstash Redis
  // מחובר (ראה SETUP.md) — אין כאן שום דבר למלא.
  //
  // חלופה: מי שמעדיף Firebase Realtime Database יכול למלא כאן
  // את ההגדרות, והאפליקציה תשתמש בו במקום:
  firebase: null,
};
